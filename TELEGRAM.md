# 텔레그램 매일 알림 설정하기

매 평일 정해진 시각에, 텔레그램으로 이런 메시지가 옵니다.

```
📅 08/05(수) SOXL 주문

【08/04 마감 결과】  종가 $139.90
🎯 17주 매도 · 실현 $+394.23  (목표달성)
💰 총자산 $20,394 (+394 / +1.97%)
   보유 0건 0주 · 현금 $20,394

【오늘 넣을 주문】
🟢 LOC 매수 14주 @ $153.89  (팔 물량 없음 · 매수범위 10%)

【손절 예정】
⚠️ 6주 (2026-07-23 매수) — 1영업일 뒤 MOC 매도

⏰ 미 동부 15:50 (한국 새벽 4:50 / 서머타임 해제 시 5:50) 까지
📱 (앱 주소)
※ 종가 기준 계산입니다. 실제 체결은 증권사 기록이 우선입니다.
```

방법은 두 가지입니다. **대부분은 방법 1이면 충분합니다.**

| | 방법 1 — 내 PC | 방법 2 — GitHub |
|---|---|---|
| 설정 | 앱에서 클릭 몇 번 | 웹사이트에서 Secrets 5개 등록 |
| PC를 꺼놓으면 | **안 옴** | 옴 |
| 설정 바꾸기 | 앱에서 바로 | GitHub 들어가서 |

---

# 방법 1 — 내 PC에서 (권장)

## 1단계 — 텔레그램 봇 만들기 (5분)

봇은 알림을 보내주는 로봇 계정입니다. 텔레그램 안에서 만듭니다.

1. 텔레그램 앱에서 검색창에 **`@BotFather`** 를 칩니다.
   - 이름 옆에 파란 체크(✔) 있는 걸 고르세요. 가짜가 많습니다.
2. 대화창에 **`/newbot`** 이라고 보냅니다.
3. 봇 이름을 물어봅니다 → 아무거나. 예: `내 종사종팔`
4. 아이디를 물어봅니다 → **반드시 `bot`으로 끝나야** 합니다. 예: `jg_jongsa_bot`
   - 이미 쓰는 사람이 있으면 다시 물어봅니다. 숫자를 붙여보세요.
5. 성공하면 이런 줄이 나옵니다.

   ```
   Use this token to access the HTTP API:
   7123456789:AAH-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

   저 긴 문자열이 **봇 토큰**입니다. 복사해 두세요.

6. **만든 봇에게 아무 말이나 한 번 보내세요.** 예: `안녕`
   - 이걸 안 하면 다음 단계가 안 됩니다. 봇은 먼저 말을 건 사람에게만 보낼 수 있습니다.

> ⚠️ 토큰은 비밀번호와 같습니다. 카톡·메일·깃허브 어디에도 그냥 올리지 마세요.
> 실수로 노출했다면 BotFather에서 `/revoke` 로 새로 발급받으면 됩니다.

## 2단계 — 앱에서 연결 (2분)

종사종팔 앱을 켜고 **`🔔 알림`** 탭으로 갑니다.

1. **봇 토큰** 칸에 1단계에서 복사한 걸 붙여넣습니다.
2. **`내 chat_id 찾기`** 를 누릅니다 → 옆 칸에 숫자가 자동으로 채워집니다.
   - 안 채워지면 1단계 6번(봇에게 말 걸기)을 안 한 겁니다.
3. **`💾 봇 정보 저장`** 을 누릅니다.

## 3단계 — 확인하고 예약 켜기 (2분)

같은 탭에서 아래로 내려갑니다.

1. **`👀 메시지 미리보기`** — 내용이 맞는지 먼저 봅니다.
2. **`📨 지금 한 통 보내기`** — 텔레그램에 실제로 오는지 확인합니다.
3. **보낼 시각**을 정합니다 (기본 저녁 9시).
4. **앱 주소**를 넣으면 메시지 아래에 링크로 붙습니다. 휴대폰에서 열 때 편합니다. (선택)
5. **`⏰ 자동 발송 켜기`** 를 누릅니다.

끝입니다. 윈도우 작업 스케줄러에 `JongsaDailyNotify` 로 등록되고, 매 평일 그 시각에 옵니다.

> **이 PC가 켜져 있고 로그인돼 있어야** 보내집니다.
> 주말에는 미국장이 안 열려 금요일과 같은 내용이 또 오므로 평일만 돌립니다.

### 저녁 9시로 잡는 이유

미국장은 한국 새벽 5~6시에 마감합니다. 저녁이면 어제 종가가 이미 확정돼 있고,
오늘 주문 마감(한국 새벽 4:50)까지는 아직 한참 남아 여유 있게 주문을 넣을 수 있습니다.

### 끄고 싶을 때

같은 탭의 **`예약 끄기`** 를 누르면 됩니다.

---

# 방법 2 — GitHub에서 (PC를 꺼놔도 받고 싶다면)

GitHub 서버가 대신 돌려줍니다. 대신 설정을 웹사이트에서 해야 합니다.

봇 토큰과 chat_id는 **방법 1의 1~2단계와 똑같이** 구합니다.
chat_id는 앱의 `🔔 알림` 탭에서 찾은 숫자를 그대로 쓰면 됩니다.

## Secrets 등록

1. 저장소 → **`Settings`** → **`Secrets and variables`** → **`Actions`**
   - 바로가기: https://github.com/ddoldol3521-blip/quant-trader/settings/secrets/actions
2. 초록색 **`New repository secret`** 을 눌러 아래를 **하나씩 5번** 등록합니다.

   | Name (그대로 복사) | Secret (내 값) |
   |---|---|
   | `TELEGRAM_BOT_TOKEN` | 봇 토큰 |
   | `TELEGRAM_CHAT_ID` | chat_id 숫자 |
   | `JONGSA_START` | 시작일. 예: `2026-08-03` |
   | `JONGSA_SEED` | 시드 달러. 예: `20000` (콤마·$ 없이 숫자만) |
   | `JONGSA_APP_URL` | 배포한 앱 주소 (안 넣어도 됩니다) |

> 💡 이름은 **대문자와 밑줄까지 똑같이** 적어야 합니다. 하나라도 다르면 알림이 안 옵니다.
>
> 시드·시작일·앱 주소까지 Secrets에 넣는 이유는 **이 저장소가 공개**라서입니다.
> 파일에 적으면 누구나 볼 수 있습니다.

## 바로 확인하기

1. 저장소 위쪽 **`Actions`** 탭 → 왼쪽에서 **`종사종팔 일일 알림`**
   - 바로가기: https://github.com/ddoldol3521-blip/quant-trader/actions/workflows/jongsa-daily.yml
2. 오른쪽 **`Run workflow`** → 다시 **`Run workflow`**
3. 1~2분 기다리면 텔레그램에 옵니다.

- **초록 ✔ 인데 메시지가 안 오면** → chat_id가 틀렸거나, 봇에게 먼저 말을 안 건 경우입니다.
- **빨간 ✘** → 클릭해서 빨간 줄을 읽어보세요. 대개 Secret 이름 오타입니다.

## 나중에 바꾸기

| 바꾸고 싶은 것 | 어디서 |
|---|---|
| 시드, 시작일, 앱 주소 | Settings → Secrets → 해당 항목 `Update` |
| 종목·분할수·목표수익률·매수범위 | `.github/workflows/jongsa-daily.yml` 의 `JONGSA_...` 줄 |
| 알림 시각 | 같은 파일의 `cron: "0 12 * * 1-5"` |
| 알림 끄기 | Actions 탭 → 워크플로 → 오른쪽 `⋯` → `Disable workflow` |

**시각 바꾸는 법**: cron은 **세계표준시(UTC)** 라서 한국시간에서 **9를 뺍니다.**

| 원하는 한국시간 | 적을 값 |
|---|---|
| 저녁 7시 | `0 10 * * 1-5` |
| 저녁 9시 (현재) | `0 12 * * 1-5` |
| 밤 11시 | `0 14 * * 1-5` |

> 참고: GitHub 서버가 붐비면 예약 시각보다 5~20분 늦게 올 수 있습니다.
> 주문 마감까지 한참 남은 시각이라 문제되지 않습니다.

---

# 알아두실 점

- 이 알림은 **종가 기준으로 계산한 예상**입니다. 실제 체결은 증권사 기록이 우선입니다.
- 미국 휴장일에도 평일이면 메시지가 옵니다. 이때는 전날과 같은 내용이 옵니다.
- 손절일이 3영업일 안으로 다가온 물량은 **【손절 예정】** 으로 미리 알려줍니다.
  목표 매도는 주문만 걸어두면 알아서 체결되지만, 손절은 날짜를 직접 세야 해서 제일 놓치기 쉽습니다.
- 알림에 쓰는 설정은 **앱에 저장된 값 그대로**입니다. 시드나 시작일을 바꾸면 알림도 따라 바뀝니다.
  (GitHub에서 돌릴 때는 Secrets에 넣은 값을 씁니다.)

## 저장 위치

| 파일 | 내용 |
|---|---|
| `telegram_config.json` | 봇 토큰, chat_id |
| `jongsa_notify.json` | 보낼 시각, 앱 주소 |
| `jongsa_settings.json` | 종목·시드·시작일 등 매매 설정 |

셋 다 `.gitignore`에 있어 **깃허브에 올라가지 않습니다.**

## 명령창에서 직접 보내보기 (문제 확인용)

```powershell
cd "D:\1. 재건정보\ai빌더\3. quant-trader"; $env:JONGSA_DRY_RUN="1"; .venv\Scripts\python.exe scripts\jongsa_daily_notify.py
```

`JONGSA_DRY_RUN="1"` 은 **보내지 않고 화면에만** 보여줍니다.
실제로 보내보려면 `$env:JONGSA_DRY_RUN=""` 로 지우고 다시 실행하세요.
