# 텔레그램 매일 알림 설정하기

매일 저녁 9시(한국시간), 텔레그램으로 이런 메시지가 옵니다.

```
📅 08/05(수) SOXL 주문

【08/04 마감 결과】  종가 $139.90
🎯 17주 매도 · 실현 $+394.23  (목표달성)
💰 총자산 $20,394 (+394 / +1.97%)
   보유 0건 0주 · 현금 $20,394

【오늘 넣을 주문】
🟢 LOC 매수 14주 @ $153.89  (팔 물량 없음 · 매수범위 10%)

⏰ 미 동부 15:50 (한국 새벽 4:50 / 서머타임 해제 시 5:50) 까지
📱 (앱 주소)
※ 종가 기준 계산입니다. 실제 체결은 증권사 기록이 우선입니다.
```

PC를 꺼놔도 옵니다. GitHub 서버가 대신 돌려주기 때문입니다.

설정은 딱 **세 단계**입니다. 순서대로 따라오시면 됩니다.

---

## 1단계 — 텔레그램 봇 만들기 (5분)

봇은 알림을 보내주는 로봇 계정입니다. 텔레그램 안에서 만듭니다.

1. 텔레그램 앱에서 검색창에 **`@BotFather`** 를 칩니다.
   - 이름 옆에 파란 체크(✔) 있는 걸 고르세요. 가짜가 많습니다.
2. 대화창에 **`/newbot`** 이라고 보냅니다.
3. 봇 이름을 물어봅니다 → 아무거나. 예: `내 종사종팔`
4. 아이디를 물어봅니다 → **반드시 `bot`으로 끝나야** 합니다. 예: `jg_jongsa_bot`
   - 이미 쓰는 사람이 있으면 다시 물어봅니다. 숫자를 붙여보세요.
5. 성공하면 이런 줄이 나옵니다.

   ```
   Use this token to access the HTTP API:
   7123456789:AAH-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
   ```

   저 긴 문자열이 **봇 토큰**입니다. 메모장에 복사해 두세요.

> ⚠️ 토큰은 비밀번호와 같습니다. 카톡·메일·깃허브 어디에도 그냥 올리지 마세요.
> 실수로 노출했다면 BotFather에서 `/revoke` 로 새로 발급받으면 됩니다.

---

## 2단계 — 내 chat_id 알아내기 (2분)

봇이 "누구에게" 보낼지 알려주는 번호입니다.

1. 방금 만든 봇을 텔레그램에서 검색합니다 (4번에서 정한 아이디).
2. 대화창을 열고 **아무 말이나 한 번 보냅니다.** 예: `안녕`
   - 이 단계를 건너뛰면 다음이 안 됩니다. 봇은 먼저 말을 건 사람에게만 보낼 수 있습니다.
3. 인터넷 주소창에 아래를 칩니다. `<토큰>` 자리에 1단계의 토큰을 넣으세요.

   ```
   https://api.telegram.org/bot<토큰>/getUpdates
   ```

   예: `https://api.telegram.org/bot7123456789:AAH-xxxx/getUpdates`

4. 화면에 이런 글자가 나옵니다. `"chat":{"id":` 뒤의 숫자가 **chat_id** 입니다.

   ```
   "chat":{"id":987654321,"first_name":...
   ```

   여기서는 `987654321`. 이것도 메모장에 복사해 두세요.

---

## 3단계 — GitHub에 값 넣기 (5분)

이제 아까 적어둔 값들을 GitHub에 등록합니다.
**Secrets(비밀값)** 칸에 넣으면 저장소가 공개여도 남에게 안 보입니다.

1. 브라우저에서 내 저장소로 갑니다.
   → https://github.com/ddoldol3521-blip/quant-trader
2. 위쪽 **`Settings`** 클릭
3. 왼쪽 목록에서 **`Secrets and variables`** → **`Actions`** 클릭
4. 초록색 **`New repository secret`** 버튼 클릭
5. 아래 표를 보고 **하나씩 5번 반복**합니다.
   (Name에 이름, Secret에 값을 넣고 `Add secret`)

   | Name (그대로 복사) | Secret (내 값) |
   |---|---|
   | `TELEGRAM_BOT_TOKEN` | 1단계의 봇 토큰 |
   | `TELEGRAM_CHAT_ID` | 2단계의 숫자 |
   | `JONGSA_START` | 시작일. 예: `2026-08-03` |
   | `JONGSA_SEED` | 시드 달러. 예: `20000` (콤마·$ 없이 숫자만) |
   | `JONGSA_APP_URL` | 배포한 앱 주소 (안 넣어도 됩니다) |

> 💡 이름은 **대문자와 밑줄까지 똑같이** 적어야 합니다. 하나라도 다르면 알림이 안 옵니다.

---

## 잘 되는지 지금 바로 확인하기

내일 저녁까지 기다릴 필요 없습니다.

1. 저장소 위쪽 **`Actions`** 탭 클릭
2. 왼쪽 목록에서 **`종사종팔 일일 알림`** 클릭
3. 오른쪽 **`Run workflow`** 버튼 → 다시 **`Run workflow`**
4. 1~2분 기다리면 텔레그램에 메시지가 옵니다.

초록색 ✔가 뜨는데 메시지가 안 오면 → chat_id가 틀렸거나 2단계에서 봇에게 먼저 말을 안 건 경우입니다.
빨간색 ✘가 뜨면 → 클릭해서 빨간 줄을 읽어보세요. 대개 Secret 이름 오타입니다.

---

## 나중에 바꾸고 싶을 때

| 바꾸고 싶은 것 | 어디서 |
|---|---|
| 시드, 시작일, 앱 주소 | GitHub → Settings → Secrets → 해당 항목 `Update` |
| 종목·분할수·목표수익률·매수범위 | `.github/workflows/jongsa-daily.yml` 파일의 `JONGSA_...` 줄 |
| 알림 시각 | 같은 파일의 `cron: "0 12 * * 1-5"` |
| 알림 끄기 | Actions 탭 → 워크플로 → 오른쪽 `⋯` → `Disable workflow` |

**시각 바꾸는 법**: cron은 **세계표준시(UTC)** 라서 한국시간에서 **9를 뺍니다.**

| 원하는 한국시간 | 적을 값 |
|---|---|
| 저녁 7시 | `0 10 * * 1-5` |
| 저녁 9시 (현재) | `0 12 * * 1-5` |
| 밤 11시 | `0 14 * * 1-5` |

> 참고: GitHub 서버가 붐비면 예약 시각보다 5~20분 늦게 올 수 있습니다.
> 주문 마감(한국 새벽 4:50)까지 한참 남은 시각이라 문제되지 않습니다.

---

## 내 PC에서 직접 보내보기 (선택)

문제가 생겼을 때 확인용입니다. PowerShell에서:

```powershell
cd "D:\1. 재건정보\ai빌더\3. quant-trader"; $env:JONGSA_DRY_RUN="1"; $env:JONGSA_START="2026-08-03"; $env:JONGSA_SEED="20000"; .venv\Scripts\python.exe scripts\jongsa_daily_notify.py
```

`JONGSA_DRY_RUN="1"` 은 **보내지 않고 화면에만** 보여줍니다.
실제로 보내보려면 `$env:JONGSA_DRY_RUN=""` 로 지우고, 토큰과 chat_id를 넣으세요.

---

## 알아두실 점

- 이 알림은 **종가 기준으로 계산한 예상**입니다. 실제 체결은 증권사 기록이 우선입니다.
- 미국 휴장일에도 평일이면 메시지가 옵니다. 이때는 전날과 같은 내용이 옵니다.
- 손절일이 3영업일 안으로 다가온 물량은 **【손절 예정】** 으로 미리 알려줍니다.
  목표 매도는 주문만 넣어두면 알아서 체결되지만, 손절은 날짜를 직접 세야 해서 제일 놓치기 쉽습니다.
