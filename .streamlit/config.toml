[browser]
gatherUsageStats = false

[theme]
# 바깥은 어둡게, 글을 읽는 안쪽은 밝게 (검은 책상 위의 흰 종이).
# 글씨가 어두워야 하니 base는 light로 두고, 바깥 배경만 CSS로 어둡게 칠한다.
base = "light"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F2F4F8"
textColor = "#1B2230"
primaryColor = "#2563EB"
